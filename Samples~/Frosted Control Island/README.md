# Frosted Control Island

Sample defaults for a compact Deucarian control island with rounded-square icon buttons,
animated visibility, and a compact scrubber.

Import this sample after installing `com.deucarian.ui`, then assign the preset to your
own app-level adapter. The package owns the geometry and motion defaults; your app should
still map its own theme roles, icons, labels, and row order.
